import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    width: '100%',
    height: 48,

    backgroundColor: '#FFF',
    paddingHorizontal: 16,
    paddingVertical: 15,
    borderRadius: 8,
    borderWidth: 1,
    borderColor: '#C3C5CB',
  }
});